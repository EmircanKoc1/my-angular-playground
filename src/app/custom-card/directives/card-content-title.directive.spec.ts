import { CardContentTitleDirective } from './card-content-title.directive';

describe('CardContentTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new CardContentTitleDirective();
    expect(directive).toBeTruthy();
  });
});
