import { CardContentDescrDirective } from './card-content-descr.directive';

describe('CardContentDescrDirective', () => {
  it('should create an instance', () => {
    const directive = new CardContentDescrDirective();
    expect(directive).toBeTruthy();
  });
});
